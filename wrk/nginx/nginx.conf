# # File: /etc/nginx/conf.d/proxy.conf

# # Define an upstream group for load balancing
# upstream backend {
#     # List of backend servers 
#     server localhost:8080;
#     server localhost:8081;
    
#     # Optional: Keepalive connections to reduce overhead
#     keepalive 32;
# }

# # Server block for handling incoming requests
# server {
#     listen 80;
#     server_name localhost; # Adjust to your domain or IP if needed

#     # Access and error logs
#     access_log /var/log/nginx/proxy_access.log;
#     error_log /var/log/nginx/proxy_error.log;

#     # Location block for reverse proxy
#     location / {
#         # Proxy requests to the upstream group (load balancer)
#         proxy_pass http://backend;
        
#         # Set headers to preserve client information
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#         # Enable keepalive to upstream servers
#         proxy_http_version 1.1;
#         proxy_set_header Connection "";

#         # Optional: Timeout settings
#         proxy_connect_timeout 5s;
#         proxy_read_timeout 60s;
#         proxy_send_timeout 60s;
#     }

#     # Optional: Health check endpoint (if your app has /health)
#     location /health {
#         proxy_pass http://backend/health;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
